import { Anchors } from '@/shared/types';

import style from './about.module.css';

export function About() {
	return (
		<section className={style.about} id={Anchors.About}>
			About
		</section>
	);
}
